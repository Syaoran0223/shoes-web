# sql 语句用法
# sql = """
#      SELECT * from product p LEFT JOIN product_attr pa on  p.id = pa.product_id
#      """
# proxy = cls.query.session.execute(sql)

# list = []
#         for row in proxy:
#             list.append(dict(zip(row.keys(), row.values())))
#         return list


head  = ['货号', '备注', '尺码', '数量', '出价']